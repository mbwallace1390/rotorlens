rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All app traffic must cross the reviewed callable-function boundary.
    // Firebase Admin bypasses these rules; IAM remains a separate production gate.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
